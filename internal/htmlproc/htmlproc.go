package htmlproc

// Package htmlproc will manage HTML artifacts on the Go side (paths, embedding, sync with Python cleaning).
// TODO: wire calls into internal/services and grpcworker / contracts when flows are defined.
